#pragma once
#ifndef TYPES_H
#define TYPES_H

/* It'll be used only for expressions where you need to put <type> many times */
typedef double f64;
typedef float f32;

typedef struct Point {
   f64 x, y, z;
} Point;

#endif
